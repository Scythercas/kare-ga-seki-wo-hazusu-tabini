# 設計書｜彼が席を外すたびに

## 1. 概要

| 項目 | 内容 |
|---|---|
| タイトル | **彼が席を外すたびに** |
| ジャンル | スマホ向けノベルゲーム（選択肢分岐あり） |
| プラットフォーム | **Web / PWA**（静的サイト＋HTTPS配信。ストア審査なし、サーバー処理なし） |
| プレイ人数 | 1台の端末を2人で共有し、ゲーム開始前に自分たちの声を録音してから遊ぶ |
| 役割分担 | **1人が「彼女役」（6本）、1人が「友人役」（4本）**。プレイヤー（主人公）は2人が共有する視点でボイスなし |
| ボイス方式 | ポイントボイス（全10箇所のみ。他はテキスト表示） |
| 開発規模 | 小規模（背景数枚、立ち絵差分のみ、3Dなし） |
| 分岐条件 | 好感度／警戒度の2軸のみで自動分岐（フラグ制なし） |

本書は「録音済みの声で遊べるノベルゲーム」を、手戻りなく実装するための設計をまとめたもの。

**ファイル構成**

| ファイル | 役割 |
|---|---|
| `設計書.md` | 本書。仕様・データ構造・数値設計・技術仕様 |
| `台本.md` | 台本の原本。加筆・推敲はこちらで行う |
| `scenes.yaml` | **シーンデータの原本。セリフの編集はここで行う**（書きかたは `docs/シーンYAMLの書きかた.md`） |
| `build_scenes.py` | `scenes.yaml` → `scenes.json` を生成 |
| `scenes.json` | 4.1節の構造。**生成物なので直接編集しない**。アプリが読むのはこちら |
| `recordings.json` | 収録10本のメタデータ（4.2節）。読み上げテキスト・感情指定・収録順 |
| `validate_scenes.py` | 上記2つの整合性チェック。**台本・データ・閾値を触ったら必ず実行する** |
| `index.html` / `css/` / `js/` | 実装本体（Web/PWA） |
| `sw.js` / `manifest.webmanifest` / `icon.svg` | PWA・オフライン対応（8.6節） |
| `public/assets/` | 背景・立ち絵。原本と配信用WebPを同居させる |
| `public/assets/CREDITS.md` | **素材の出典と利用条件の記録。素材を足したら必ず追記** |
| `optimize_assets.py` | 原本から配信用WebPを生成（7.2節） |
| `docs/立ち絵生成仕様_友人.md` | 友人の立ち絵をAI生成するための仕様書 |

**js/ の構成**

| ファイル | 役割 |
|---|---|
| `js/config.js` | 5.2節の定数と各種設定。**バランス調整はここだけを触る** |
| `js/db.js` | IndexedDB（録音Blob・名前・録音メタ）と永続化要求 |
| `js/audio.js` | 録音（`MediaRecorder`）と再生（`playVoice` / `stopVoice`） |
| `js/engine.js` | シーン進行・条件評価・分岐解決。`scenes.json` を解釈する |
| `js/app.js` | 画面遷移と全体の組み立て |

**動かし方**

ES モジュールと `getUserMedia` を使うため、**HTTPサーバー経由で開く必要がある**（`index.html` をファイルとして直接開いても動かない）。

```
python -m http.server 8000
# → http://localhost:8000 を開く
```

セリフやシーンを編集したら、必ず次を実行してから確認すること。

```
python build_scenes.py     # scenes.yaml → scenes.json
python validate_scenes.py  # 192ルート走査・到達分布・整合チェック
```

マイクはセキュアコンテキストでのみ使えるため、スマホ実機で試す場合は HTTPS で配信するか、PC とスマホで開発用トンネル等を使う。

---

## 2. スコープ定義（今回実装する範囲／後回しにする範囲）

今回の要望に基づき、優先度を明確に分けておく。実装時に範囲がぶれないようにするための最重要事項。

### 今回実装する（MVP）
- 台本に沿ったテキスト表示（地の文・セリフ・選択肢）
- 選択肢による好感度／警戒度の数値管理と分岐
- **友人役の名前入力**（録音前に1回。台本の `{{friendName}}` に反映）
- ゲーム開始前の**録音機能**（10セリフ分、マイク入力→端末内保存）
- **録音直後のプレビュー再生機能**（本人確認用）
- **確定前の上書き録音**（プレビューを聞いて気に入らなければ、確定を押す前は何度でも録り直せる）
- 本編再生時、該当シーンで対応する録音ファイルを自動再生
- 4エンディングへの自動振り分け
- **周回プレイ**（エンド後、録音を保持したまま本編だけやり直す）

### 今回は実装しない（将来対応・設計上は考慮のみ）
- **確定後**の録り直し（10本の一覧画面から任意の1本を選んで再録音する機能）
- 音量／無音チェックなどの録音品質チェック
- 録音データのリセット／別ペアでの再プレイ機能
- サーバーへの録音データ送信・共有機能
- 誰が男性役／女性役かを選ぶUI（今回は「彼女役→友人役」の固定順で収録）
- **本編途中のセーブ／中断復帰**（タブを閉じたら本編は最初から。録音と名前は保持されるためすぐ再開できる）

**設計方針**：上記「後回し」項目は、録音データの管理を汎用的な構造（後述のデータ構造）にしておくことで、後から機能追加してもコアロジックの作り直しが発生しないようにする。

### 2.1 意図的にそう決めた仕様（レビュー時に「バグ」と誤認しないための記録）

以下は検討の結果、**あえてこの仕様にしている**もの。指摘を受けた際はここを参照する。

| 項目 | 決定内容 | 理由 |
|---|---|---|
| エンディングのネタバレ | 開始前に10本すべて録音する。4エンドのセリフも先に読み上げるため、結末は全員が知った状態でプレイが始まる | 実装を最小に保つため。遊びの軸を「結末を知らずに読む」から「**この2人はどのエンドに着地するのか**」に置き換えて成立させる |
| プレイヤーのボイスなし | 主人公は画面の前の2人が共有する視点とし、専用の声を持たない | 2人＝彼女役＋友人役に固定。役割選択UIが不要になり、収録本数も増えない |
| 鳴らない録音がある | voice_02・03 は該当選択時のみ再生。1プレイで鳴るのは10本中4〜6本 | 全部聞くには選択を変えて周回する必要がある＝周回プレイの動機として利用する |
| 好感度・警戒度のクランプなし | 負値を許容する（0で止めない） | 0クランプすると初期値0のため Scene1-B の「警-1」が完全に無効化されるため |
| 確定後は録り直せない | 救済は「確定前の上書き録音」までとし、確定後の再録音画面は作らない | プレビューで確認したうえで確定する導線があれば実用上足りるため。データ構造だけ将来対応にしておく |
| シーン数を9のまま据え置く | 本編1周を約10分にするにあたり、シーンを追加せず既存9シーンのテキストを厚くする方法を採った | シーンを足すと選択肢・数値設計・到達分布・背景素材のすべてに波及する。テキスト追加なら192ルートもボイス10本も一切変わらず、手戻りがゼロで済むため |

### 2.2 体験時間の設計（目標：本編1周＝約10分）

| 工程 | 所要 |
|---|---|
| 名前入力 | 〜30秒 |
| 録音（10本／読み上げ＋プレビュー＋確定） | **約5分**（録り直しが入ればさらに延びる） |
| **本編1周** | **約9〜11分** |
| 初回合計 | **約15分** |
| 2周目以降 | 約9〜11分（録音は再利用） |

**本編1周の内訳（実測ベース）**

| 要素 | 分量 | 時間 |
|---|---|---|
| テキスト（地の文＋セリフ） | **3,429字**（1周あたり。Scene9は4エンド中1本のみ通過するため、台本全体の文字数とは一致しない） | 6.9分（500字/分）／8.6分（400字/分） |
| 選択肢の相談 | 7回 × 約15秒（2人で相談する前提） | 約1.75分 |
| ボイス再生 | 1周4〜6本 × 5〜8秒 | 約0.7分 |
| **合計** | | **約9〜11分** |

- **テキスト量を調整する際は「1周あたりの文字数」で数えること**。台本全体の文字数にはエンド4本分が含まれており、1周では1本しか通らないため約1.5割ぶん多く見える
- 目安：**500字＝約1分**。尺を1分伸ばしたいならScene1〜8に500字足す（エンド部分に足しても1周には約1/4しか効かない）
- 録音（約5分）に対して本編（約10分）が上回る構成になった。ただし録音画面のテンポ（ボタン数・待ち時間・自動遷移）が体験の質を直接左右することに変わりはないため、**UIの作り込みは録音画面を優先する**

---

## 3. 全体フロー

```
[アプリ起動]
    ↓
[名前入力]  友人役の名前を入力 → 以降 {{friendName}} に反映（録音後は変更不可）
    ↓
[録音モード]  ※voiceId順ではなく話者ごとにまとめた収録順で進行
  彼女役 1/10 → 台本表示 → 録音 → プレビュー再生
                              ├→「録り直す」→ 上書き録音（確定前は何度でも）
                              └→「確定」→ 次へ
   ...（voice_01,02,03,04,09,10 の6本）
  ── 持ち替え画面（1回だけ）──
  友人役 7/10 →（同上）
   ...（voice_05,06,07,08 の4本）
    ↓
[プレイ開始ボタン活性化]  ※10本すべて confirmed のときのみ
    ↓
[本編プレイ]  ←──────────────────┐
  Scene1 遭遇（警）                    │
   → Scene2 カフェ                     │
   → Scene3 離脱①（好・🎤②）           │
   → Scene4 再開①（警）                │
   → Scene5 離脱②（好・🎤③）           │
   → Scene6 再開②（警）                │
   → Scene7 離脱③（好・警・🎤④）        │
   → Scene8 解散前（警 → 判定 → 🎤⑤/⑥） │
    ↓                                  │
[Scene9 エンディング（自動分岐）]        │
    ↓                                  │
[「もう一度遊ぶ」] ──────────────────┘
  ※変数のみリセット。録音と名前は保持する
```

---

## 4. データ構造設計

### 4.1 セリフ・シーンデータ（台本本体）

**原本は `scenes.yaml`**（人が書く）。`build_scenes.py` が以下のJSON構造に展開し、アプリは `scenes.json` を読む。
ブラウザにYAMLパーサを積まずに済み、実行時の挙動は変わらない。**`scenes.json` は生成物なので直接編集しないこと。**

```yaml
# scenes.yaml（原本）— 素の文字列は地の文、彼女:／友人: はセリフ
  - id: scene_03
    見出し: 離脱①（トイレ）
    背景: bg_cafe
    本文:
      - 「ちょっとトイレ」と友人が席を立つ。
      - 友人: お、店員さん呼んどいてくれる？水もらいたいから
        表情: { 友人: normal, 彼女: normal }
    選択肢:
      id: choice_03
      options:
        - A: 「（店員に）すみません、お水いただけますか」
          好感度: 1
        - B: 何も言わず様子を見る
    選択後:
      - 彼女: ……ありがとう。{{friendName}}、ああいうところ、ちょっと抜けてるんだよね
        声: voice_02
        表情: smug
        条件: choice_03 == A
    次: scene_04
```

これが下記のJSONに展開される。

```json
{
  "scenes": [
    {
      "id": "scene_03",
      "title": "離脱①（トイレ）",
      "stage": { "bg": "bg_cafe", "bgm": "bgm_daily" },
      "text": [
        { "speaker": "narration", "body": "「ちょっとトイレ」と友人が席を立つ。..." },
        { "speaker": "friend", "body": "お、店員さん呼んどいてくれる？水もらいたいから",
          "sprite": { "friend": "normal" } }
      ],
      "choice": {
        "id": "choice_03",
        "se": "se_select",
        "options": [
          { "key": "A", "label": "（店員に）すみません、お水いただけますか",
            "effect": { "affection": 1 } },
          { "key": "B", "label": "何も言わず様子を見る",
            "effect": {} }
        ]
      },
      "after": [
        { "speaker": "girlfriend", "body": "……ありがとう。{{friendName}}、ああいうところ、ちょっと抜けてるんだよね",
          "voiceId": "voice_02",
          "sprite": { "girlfriend": "surprised" },
          "condition": { "choice": "choice_03", "equals": "A" } }
      ],
      "next": "scene_04"
    }
  ]
}
```

**フィールド定義**

| フィールド | 内容 |
|---|---|
| `speaker` | `narration` / `friend` / `girlfriend`（`player` は選択肢ラベルで表現するため、行としては持たない） |
| `body` | 表示テキスト。`{{friendName}}` を含む場合は表示直前に置換する（4.4節） |
| `voiceId` | ボイスがある行のみ付与。録音データテーブルの `voiceId` と突き合わせる |
| `condition` | **その行を再生するか否かの条件**。`{ "choice": "<choice_id>", "equals": "A" }`（選択依存）または `{ "var": "warning", "op": ">=", "value": "WARNING_THRESHOLD" }`（変数依存）。省略時は常に再生 |
| `effect` | 選択時の好感度(`affection`)／警戒度(`warning`)の増減 |
| `next` | 次のシーンID。条件分岐がある場合は配列で `[{ "condition": {...}, "to": "..." }, { "to": "..." }]`（先頭から評価し、条件なしをフォールバックとする） |
| `stage` | そのシーン**開始時**の背景・BGM。前シーンから変化がない場合は省略可 |
| `bg` | **行レベルの背景変更**。シーンの途中で場所が変わる行にだけ付ける（Scene4の「店を出て、3人で街をぶらぶらする」でカフェ→街中に切り替わる） |
| `bgm` | **行レベルのBGM変更**。`null` は停止（無音）。Scene7で🎤④の直前に `bgm_honne` へ切り替えるために必要。①バレエンドの無音演出も `null` で表現する |
| `sprite` | 行ごとの立ち絵差分指定。`{ "girlfriend": "surprised" }` の形式。**値が `null` ならその立ち絵を画面から外す**（離脱シーンで友人が不在になる場合）。指定のないキャラは直前の状態を維持する |
| `se` | 効果音（選択肢決定音、シーン切り替えなど） |
| `after` | 選択肢の**後**に表示・再生する行。Scene3・5の条件付きボイスに加え、**条件付きの地の文**（Scene4の「（Bの場合）友人が一瞬だけ、こちらを見た」）もここに入る。`condition` はボイス専用ではなく、ボイスなしの行にも付く |

**選択結果の保持**：`condition` が選択肢を参照するため、プレイ中に選んだ結果を `{ "choice_03": "A", ... }` の形で保持する。エンディング判定には使わない（判定は2変数のみ）が、条件付きボイスの解決に必須。

**条件付き行の一覧（実装チェックリスト）**

`condition` が必要な行は全部で9つ。ボイスだけでなく地の文にも付くため、取りこぼしやすい。

| # | 場所 | 種別 | 条件 |
|---|---|---|---|
| 1 | Scene3 🎤② voice_02 | ボイス＋セリフ | choice_03 == A |
| 2 | Scene3 「その『ありがとう』は……」 | **地の文** | choice_03 == A |
| 3 | Scene4 「テーブルには新しい水のグラスが……」 | **地の文** | choice_03 == A |
| 4 | Scene4 「頼まれていた水はそのままで……」 | **地の文** | choice_03 == B |
| 5 | Scene4 「友人が一瞬だけ、こちらを見た」 | **地の文** | choice_04 == B |
| 6 | Scene5 🎤③ voice_03 | ボイス＋セリフ | choice_05 == A |
| 7 | Scene5 「少しだけ声が大きくなって……」 | **地の文** | choice_05 == A |
| 8 | Scene8 🎤⑤ voice_05 | ボイス＋セリフ | warning < WARNING_THRESHOLD |
| 9 | Scene8 🎤⑥ voice_06 | ボイス＋セリフ | warning ≧ WARNING_THRESHOLD |

※ 上表の2〜5・7は**ボイスのない条件付き地の文**。条件判定をボイス再生処理の中に実装するとこれらが漏れるため、**条件判定は「行を表示するか」の層で行い、ボイス再生はその後段に置く**こと。

**3と4は排他ペア**（Scene3でAを選べば水は来ており、Bなら来ていない）。Scene3の選択がScene4冒頭の描写に効く形になっており、選ばなかった側の結果が見えることで選択に手応えが出る。**片方だけ条件を書くと、もう片方のルートで冒頭が欠落する**ので必ず対で扱う。

### 4.2 録音データ管理

```json
{
  "recordings": [
    {
      "voiceId": "voice_01",
      "recordingOrder": 1,
      "speakerRole": "girlfriend",
      "sceneRef": "scene_01",
      "scriptText": "あ……{{friendName}}くんの……友達、だよね？前に写真で見たことある気がする",
      "emotionNote": "少し驚き＋人見知り気味の挨拶",
      "blobKey": "rec_voice_01",
      "mimeType": "audio/mp4",
      "durationMs": 4200,
      "recordedAt": "2026-08-22T10:00:00+09:00",
      "status": "confirmed"
    }
  ]
}
```

| フィールド | 内容 |
|---|---|
| `voiceId` | 台本側と紐付ける**不変の識別子**。並び順の意味は持たせない |
| `recordingOrder` | **収録画面での提示順**（1〜10）。話者ごとにまとめるため voiceId 順とは一致しない。将来「確定後の録り直し」「順序変更」を入れてもここだけ触れば済む |
| `speakerRole` | `girlfriend` / `friend`。持ち替え画面の挿入判定に使う |
| `scriptText` | 収録画面に表示する読み上げ用テキスト。`{{friendName}}` は置換して表示する |
| `blobKey` | IndexedDB 上の録音Blobのキー（8.3節）。**サーバーには送信しない** |
| `mimeType` | 録音時に実際に採用された MIME タイプ。**ブラウザによって変わるため必ず記録する**（8.2節） |
| `durationMs` | 録音長。プレビュー表示と、将来の品質チェック（無音検出等）の足場 |
| `status` | `unrecorded` → `recorded` → `confirmed` の3状態（下記） |

**status の遷移**

```
unrecorded ──[録音]──→ recorded ──[確定]──→ confirmed
                          ↑    │
                          └────┘
                        [録り直す] ※上書き。確定前のみ
```

- `recorded`：録音済みだが未確定。プレビュー再生と上書き録音ができる。**この状態を持たせることが「確定前の録り直し」の実装の中心**
- `confirmed`：確定済み。今回は変更不可。将来「確定後の録り直し」を足す場合は `confirmed → recorded` の遷移を1本追加するだけで済む
- 「はじめる」ボタンの活性条件は **10本すべてが `confirmed`**

**収録順マッピング**

| recordingOrder | voiceId | speakerRole |
|---|---|---|
| 1〜6 | voice_01, 02, 03, 04, 09, 10 | girlfriend |
| 7〜10 | voice_05, 06, 07, 08 | friend |

- 端末の持ち替えは `recordingOrder` が 6→7 に変わる**1回だけ**。この境目で「次は◯◯役の方に代わってください」の画面を挟む
- 進捗表示は `recordingOrder` ベース（例：`3 / 10`）

### 4.3 ゲーム内変数（分岐用）

```json
{
  "affection": 0,
  "warning": 0,
  "choices": { "choice_01": "A", "choice_03": "A" }
}
```

| 変数 | 初期値 | 到達範囲 | 変動シーン |
|---|---|---|---|
| `affection`（好感度） | 0 | **-1 〜 +5** | Scene3・Scene5・Scene7 |
| `warning`（警戒度） | 0 | **-1 〜 +4** | Scene1・Scene4・Scene6・Scene7・Scene8 |

- **クランプは行わない**（負値を許容する）。0で下限を切ると Scene1-B の `warning -1` が初期値0のため無効化され、選択肢が実質死ぬ
- エンディング判定は終了時にこの2値を参照するのみ（フラグ変数は持たない）
- `choices` は条件付きボイス（voice_02 / voice_03）の解決専用

**周回時のリセット対象**（「もう一度遊ぶ」を押したとき）

| データ | 扱い |
|---|---|
| `affection` / `warning` / `choices` | **リセットする**（初期値に戻す） |
| `recordings`（録音Blobを含む） | **保持する** |
| `friendName` | **保持する** |

この切り分けを1つの関数（例：`resetPlaythrough()`）に閉じ込めておく。将来「別ペアで最初から」を足す場合は、録音と名前も消す `resetAll()` を別に用意すればよい。

### 4.4 名前プレースホルダ（`{{friendName}}`）

起動直後に**友人役の名前**を1つだけ入力させ、台本中の `{{friendName}}` を置換する。

| 項目 | 仕様 |
|---|---|
| 入力項目 | 友人役の名前 1つのみ（プレイヤー名・彼女名は台本上どこにも出てこないため不要） |
| 出現箇所 | 台本中に4箇所。うち**2箇所はボイス収録対象**（🎤① voice_01 / 🎤⑨ voice_09） |
| 置換タイミング | 表示・収録テキスト生成の直前。データ側は `{{friendName}}` のまま保持する（後から置換方式を変えられるように） |
| バリデーション | 空欄不可。1〜8文字程度の上限を設ける（テキストボックスのレイアウト崩れ防止） |
| 敬称 | 台本側に含める（🎤①は `{{friendName}}くん`）。敬称を変数に含めない |
| **変更の可否** | **録音開始後は変更不可**。名前を変えると、すでに録音済みの音声（voice_01/09）と画面のテキストが食い違うため。名前入力は録音画面より前に置き、以降は編集導線を出さない |

保存先は録音データと同じ永続ストレージ（8.3節）。周回時は保持する。

---

## 5. 数値設計とエンディング分岐

### 5.1 選択肢の効果値（確定）

| Scene | 選択肢 | affection | warning | 付随する挙動 |
|---|---|---|---|---|
| 1 | A．いいね、ちょうど暇してた | ±0 | ±0 | |
| 1 | B．お邪魔じゃないか？ | ±0 | **-1** | 負値になる唯一の箇所 |
| 3 | A．店員にフォローを入れる | **+1** | ±0 | 🎤② voice_02 を再生 |
| 3 | B．何も言わず様子を見る | ±0 | ±0 | voice_02 は再生されない |
| 4 | A．友人に話を振り返す | ±0 | ±0 | |
| 4 | B．彼女の言葉に先に反応する | ±0 | **+1** | |
| 5 | A．素直に共感する | **+1** | ±0 | 🎤③ voice_03 を再生 |
| 5 | B．当たり障りなく相槌 | ±0 | ±0 | voice_03 は再生されない |
| 6 | A．そんなことないだろ | ±0 | ±0 | |
| 6 | B．苦笑いだけで流す | ±0 | **+1** | |
| 7 | A．気持ちに応える | **+3** | **+1** | 唯一のトレードオフ選択 |
| 7 | B．一歩引いて流す | **+1** | ±0 | |
| 7 | C．はっきり距離を置く | **-1** | ±0 | |
| 8 | A．何事もなく友人へ | ±0 | ±0 | この直後に警戒度が確定 |
| 8 | B．一瞬動けない | ±0 | **+1** | 同上 |

**設計意図**：警戒度は Scene1・4・6・7A・8 の5箇所に分散させ、「1つの選択で決まる」状態を避けた。好感度は Scene7 が支配項（+3）だが、Scene3・5 の積み重ね（最大+2）が④成功エンドの到達可否を左右するため、序盤の選択が無意味にならない。

### 5.2 閾値定数（暫定値・テストプレイ後に調整）

```
WARNING_THRESHOLD = 3    # warning がこの値以上なら①バレエンド
AFFECTION_LOW     = 2    # これ未満なら②進展なしエンド
AFFECTION_HIGH    = 4    # これ以上なら④成功エンド
```

```
if warning >= WARNING_THRESHOLD:
    → ①バレエンド
elif affection < AFFECTION_LOW:
    → ②進展なしエンド
elif affection < AFFECTION_HIGH:
    → ③拒否エンド
else:
    → ④成功エンド
```

- 3つの定数は**必ず1箇所にまとめる**（例：`config.js`）。バランス調整時にコード全体を探さなくて済むようにするため
- 別名の閾値を派生させない（理由は5.4節）

### 5.3 全192ルートの到達エンド分布

選択の組み合わせは `2×2×2×2×2×2×3 = 192通り`。5.2の暫定値での分布は以下。

| エンド | 到達ルート数 | 割合 | 主な到達条件 |
|---|---|---|---|
| ①バレエンド | 28 | 14.6% | warning ≧ 3。Scene7でAを選んだ場合は残り4箇所中2箇所、選ばなかった場合は4箇所すべてで「怪しい」側を選ぶ必要がある |
| ②進展なしエンド | 75 | 39.1% | Scene7でCを選べば必ずここ。Bを選び、かつ序盤も積んでいない場合も該当 |
| ③拒否エンド | 56 | 29.2% | Scene7でB（＋序盤の積み上げ）、またはAでも序盤をまったく積んでいない場合 |
| ④成功エンド | 33 | 17.2% | Scene7でA、かつScene3・5のいずれかで好感度を取っていること |

**この分布から読み取れること**
- ④成功エンドは「山場でAを選ぶ」だけでは届かず、序盤の積み上げが1つ以上必要（affection=3では③拒否になる）。狙いどおりの設計
- ②が最も多いのは Scene7-C が単独で②に直結するため。②の到達率を下げたい場合は `AFFECTION_LOW = 1` に変更する
- ①は最もレアな結末（14.6%）。バレエンドの到達率を上げたい場合は `WARNING_THRESHOLD = 2` にする

### 5.4 Scene8（友人セリフ）とエンディング分岐の整合性【重要】

Scene8の友人セリフ（voice_05／voice_06のどちらを再生するか）は、**エンディング判定と同じ`WARNING_THRESHOLD`定数を必ず参照する**。別の閾値を独自に定義しない。

```
# Scene8：選択肢 → warning 確定 → 以下を評価
if warning >= WARNING_THRESHOLD:
    → voice_06（警戒バージョン）を再生 → この後①バレエンドへ
else:
    → voice_05（通常バージョン）を再生 → この後②③④のいずれかへ
```

理由：もし将来的な数値調整でScene8用としてWARNING_THRESHOLDとは別の閾値（例：`WARNING_THRESHOLD_SCENE8 = 5`）を作ってしまうと、「voice_06（友人が疑うセリフ）を言ったのに、結果はバレなかった（②③④に分岐した）」あるいはその逆の矛盾が発生しうる。閾値を1つの定数に統一することでこの矛盾を構造的に防ぐ。

**実装上の順序制約**：Scene8は「選択肢 → warning確定 → ボイス選択 → エンディング判定」の順で処理する。選択肢をボイス再生より後ろに置くと、Scene8の選択が警戒度に反映されないままボイスが決まり、上記の矛盾がそのまま発生する。

**組み合わせ確認表（実装後のQA項目として使用）**

| warning | 判定 | Scene8ボイス | エンディング | 整合性 |
|---|---|---|---|---|
| ≧ WARNING_THRESHOLD | 警戒 | voice_06（疑うセリフ） | ①バレエンド | ✅ 一致 |
| < WARNING_THRESHOLD かつ affection < AFFECTION_LOW | 平常 | voice_05（通常セリフ） | ②進展なしエンド | ✅ 一致 |
| < WARNING_THRESHOLD かつ AFFECTION_LOW ≦ affection < AFFECTION_HIGH | 平常 | voice_05（通常セリフ） | ③拒否エンド | ✅ 一致 |
| < WARNING_THRESHOLD かつ affection ≧ AFFECTION_HIGH | 平常 | voice_05（通常セリフ） | ④成功エンド | ✅ 一致 |

実装後、この4パターンを実際にプレイして表の通りになるかを確認する（デバッグ用に`affection`・`warning`を画面に一時表示できるようにしておくと確認が早い）。

**各エンドへの確認ルート（QA用）**

| エンド | 選択手順 | 最終値 |
|---|---|---|
| ①バレエンド | 1A → 3B → 4B → 5B → 6B → 7A → 8A | affection=3 / warning=3 |
| ②進展なしエンド | 1A → 3B → 4A → 5B → 6A → 7C → 8A | affection=-1 / warning=0 |
| ③拒否エンド | 1A → 3A → 4A → 5B → 6A → 7B → 8A | affection=2 / warning=0 |
| ④成功エンド | 1A → 3A → 4A → 5A → 6A → 7A → 8A | affection=5 / warning=1 |

※ ①のルートは voice_02・voice_03 が再生されないパターンでもあるため、「鳴らないボイスがある」挙動の確認も同時に行える。周回機能を使えばこの4ルートを録音し直さずに順に確認できる。

---

## 6. 画面構成

| 画面 | 役割 |
|---|---|
| タイトル／名前入力画面 | 友人役の名前を入力（1〜8文字・空欄不可）→ 録音へ。**録音開始後はこの画面に戻さない** |
| マイク権限説明 | 「これから声を録ります」の説明＋許可ボタン（ブラウザのダイアログを出す前に一言挟む）。拒否された場合の再許可手順もここに表示 |
| 録音画面 | 読み上げテキスト／話者ロール表示／感情指定／録音ボタン／プレビュー再生ボタン／**録り直すボタン**／確定ボタン／進捗（例：3/10） |
| 持ち替え画面 | `recordingOrder` 6→7 の境目で挟む「次は◯◯役の方に代わってください」表示 |
| **遊びかた／プレイ開始画面** | 10本すべて `confirmed` になった後に表示。**何を目指すゲームなのかをここで説明する**（下記）。「はじめる」のタップを音声再生の起点にする（8.4節） |
| 本編画面 | 背景／立ち絵／テキストボックス／選択肢ボタン／（該当箇所で自動でボイス再生） |
| エンディング画面 | 分岐に応じた締めのテキスト＋ボイス再生／**「もう一度遊ぶ」ボタン** |

### 6.1 遊びかたの説明（プレイ開始画面）

本作は目的が自明ではない。「選択肢を選ぶと何が起きるのか」「何を目指せばいいのか」が分からないままでは、選択に意味を感じられない。**本編に入る直前に、以下を説明する。**

| 伝えること | 文面の要点 |
|---|---|
| 立場 | 友人とその恋人と3人で過ごす4時間の主人公。友人は何度か席を外す |
| **好感度** | 彼女があなたに惹かれていく度合い。気遣いや共感で上がる |
| **警戒度** | 友人が怪しむ度合い。二人の距離が態度に出ると上がる |
| **トレードオフ** | 距離を詰めるほど良い結末に近づくが、表に出れば気づかれる ← **ゲームの中心** |
| ゴール | 結末は4つ。**どこに着地するかを見届ける**のが遊びの軸 |
| 周回 | 1回で流れるボイスは10本中4〜6本。選択を変えると未聴の声が流れる |

**数値そのものは見せない**（デバッグ表示を除く）。2つの軸が存在することと、それが相反することだけを伝える。
2.1節のとおり結末は録音時点で既知なので、「どれを引くか」を能動的に狙わせる説明にすることで、ネタバレを遊びの一部に転化させる。

### 6.2 ボイス再生中のテキスト表示と操作

| 項目 | 仕様 |
|---|---|
| テキスト表示 | **左から1文字ずつ送る**（タイプライタ表示）。速度は `js/config.js` の `TYPING_MS`（既定32ms／文字）。`、`『。』`…` のあとだけ `TYPING_PAUSE` ぶん溜めて読みのリズムを作る |
| タップ操作 | **1回目のタップで残りを一気に表示、2回目のタップで次の行へ**。ノベルゲームの標準的な挙動 |
| ボイスとの関係 | **読み上げに同期させない。** 表示速度はテキスト側の都合で決め、ボイスは独立に再生する |
| 同期させない理由 | 収録するのはプロの声ではないため、言い直しが入る・尺が人によって大きく変わる。音声に同期させると破綻する。文字送りが先に終わってもボイスは鳴り続けてよい |
| 送り記号 | 表示しきったら `#next-mark`（▼）が点滅する。まだ表示途中かどうかが分かる |
| スキップ | 表示中でも次の行へ進める（2タップ）。周回時にテンポを落とさないため |
| 実装 | 再生・停止は `playVoice` / `stopVoice` の2つに集約し、画面遷移・選択肢・エンディングでは `cancelTyping()` で文字送りも止める。停止漏れがあると次のシーンに前のボイスが被る |

### 6.3 録音画面のボタン状態

| status | 録音ボタン | プレビュー | 録り直す | 確定 |
|---|---|---|---|---|
| `unrecorded` | 活性 | 非活性 | 非表示 | 非活性 |
| `recorded` | 非表示 | 活性 | 活性 | 活性 |
| `confirmed` | ―（次の1本へ自動遷移） | ― | ― | ― |

---

## 7. 素材リストと演出割り当て

素材の出典・利用条件は `public/assets/CREDITS.md` に記録する。**素材を追加したら必ず追記すること。**

### 7.1 素材リスト

| 種別 | 状態 | 内訳 |
|---|---|---|
| 背景 | ✅ **4枚（みんちりえ）** | `bg_station`（街中の踏切・日中）／`bg_cafe`（飲食店の店内・日中）／`bg_town_day`（商店街・日中）／`bg_town_evening`（商店街・夕方） |
| 立ち絵（彼女） | ✅ **7表情（AI生成）** | `normal` / `away` / `bored` / `fluster` / `laugh` / `smile` / `smug` |
| 立ち絵（友人） | ⬜ **未作成** | `normal` / `suspicious`（必須）、`smile` / `cold`（推奨）。生成仕様は `docs/立ち絵生成仕様_友人.md` |
| BGM | ✅ **4曲（甘茶の音楽工房）** | `bgm_daily`（夏色のキャンパス）／`bgm_evening`（夕暮れのキャンパス）／`bgm_honne`（現実の隙間）／`bgm_ending`（月明かりの灯台） |
| 効果音 | ✅ **4点（効果音ラボ）** | `se_select`（選択肢決定）／`se_confirm`（録音確定）／`se_rec_start`（録音開始の合図）／`se_rec_stop`（録音停止） |
| プレイヤーの立ち絵 | — | **不要**（一人称視点のため画面に出さない） |

**背景が4枚で足りる理由**：`bg_town_day` と `bg_town_evening` は**同じ商店街の時間差分**を使っている。Scene4〜5（昼）から Scene6〜8（夕方）へ、同じ場所のまま時間だけが進む見せ方になり、「日が傾き始める」が絵で伝わる。Scene7の売店も商店街の中として自然に読める。

**`bgm_daily` と `bgm_evening` は同じ作者の昼／夕方の対になる曲**。背景（商店街の昼／夕方）の時間差分と揃えてあり、Scene6で背景と同時に切り替わる。当初3曲の想定だったが、対の曲が存在したため4曲にした。

**効果音を4点に絞った理由**：テキスト送り音は入れていない。タイプライタ表示と組み合わせるとタップのたびに鳴って耳障りになり、静かな作品のトーンを壊すため。シーン切り替え音も入れていない（BGMの切り替えが同じ役割を果たすので冗長）。
一方、**録音の開始・停止には合図を入れた**。このゲームの核が録音であり、合図がないと「いつから喋ればいいか」が分からず頭が切れるため。`se_rec_start` は**鳴らし終えてからマイクを開く**（`SE_REC_CUE_MARGIN_MS` の余白つき）。順序を逆にすると合図そのものが録音の頭に入る。

**①バレエンドは専用BGMを用意しない**。`bgm` に `null` を指定して**停止（無音）**で処理する。曲数を増やさずに最も落差が出る。

**曲の切り替わり**

| Scene | BGM |
|---|---|
| 1〜5 | `bgm_daily`（通しで鳴り続ける。同じ曲名の間は再生を継続する実装） |
| 6 | `bgm_evening`（背景の夕方化と同時） |
| 7 | 「彼女の表情が、今までと少し違う」の行で `bgm_honne` に切替（🎤④の直前） |
| 8 | `bgm_evening` に戻す（何事もなかったかのように） |
| 9-① | **停止（無音）** |
| 9-② | `bgm_daily`（日常が戻る） |
| 9-③④ | `bgm_ending` |

**友人の立ち絵が未用意の間**、ゲーム内では差分名を表示するプレースホルダに自動で切り替わる（`js/app.js` の `setSprite`）。1枚だけ置いた状態でも動作する。

### 7.2 配信用ファイルの扱い

原本（立ち絵PNG・背景JPEG）はリポジトリに残し、**アプリが読むのは `optimize_assets.py` が生成した `.webp`**。

```
python optimize_assets.py
```

| 種別 | 原本 | 配信用 |
|---|---|---|
| 立ち絵・背景 | 13.0 MB (PNG/JPEG) | **1.40 MB** (WebP) |
| BGM | 8.5 MB (MP3 128kbps) | **6.47 MB** (AAC 96kbps / .m4a) |
| 効果音 | 0.11 MB (MP3) | **0.08 MB** (WAV モノラル22.05kHz) |
| **合計** | 21.6 MB | **7.94 MB**（上限10MBに対して79%） |

**効果音だけWAVなのは意図的**。0.2秒程度のUI音にMP3/AACのエンコード前詰め（数十msの無音）が乗ると、押した瞬間の反応が鈍って感じられる。短いのでWAVでも4点合計77KBにしかならない。

BGMを **AAC(.m4a)** にしているのは、iOS Safari を含めて全ブラウザで再生できるため。Opus のほうが小さいが Safari の対応が不確実で、8.4節の実機要件と噛み合わない。

素材を追加・差し替えたら、①`optimize_assets.py` を実行 → ②`sw.js` の `ASSETS` 一覧を更新 → ③`CACHE_VERSION` を上げる、の3手順が必要。

### 7.3 演出割り当て（背景・BGM）

| Scene | 背景 | BGM |
|---|---|---|
| 1 遭遇 | `bg_station` | `bgm_daily` |
| 2 カフェ | `bg_cafe` | `bgm_daily` |
| 3 離脱① | `bg_cafe` | `bgm_daily` |
| 4 再開① | **`bg_cafe` で開始し、「店を出て、3人で街をぶらぶらする」の行で `bg_town_day` に切替**（シーン内で場所が変わる唯一の箇所） | `bgm_daily` |
| 5 離脱② | `bg_town_day` | `bgm_daily` |
| 6 再開② | `bg_town_evening` | `bgm_daily` |
| 7 離脱③（山場） | `bg_town_evening` | **🎤④の直前の行で `bgm_honne` へ切替** |
| 8 解散前 | `bg_town_evening` | `bgm_daily` に戻す（何事もなかったかのように） |
| 9-① バレエンド | `bg_town_evening` | **停止（`null`）** |
| 9-② 進展なし | `bg_town_day`（数週間後の場面のため夕方ではない） | `bgm_daily` |
| 9-③ 拒否 | `bg_town_evening` | `bgm_ending` |
| 9-④ 成功 | `bg_town_evening` | `bgm_ending` |

### 7.4 演出割り当て（立ち絵）

表情が変わる行のみ記載。記載のない行は直前の差分を維持する。`null` は画面から外す（離脱シーンで友人が不在）。

| 箇所 | 友人 | 彼女 | 根拠 |
|---|---|---|---|
| Scene1 🎤① | ― | **`fluster`** | 少し驚き＋人見知り気味 |
| Scene1 友人セリフ | `normal` | `normal` | |
| Scene2 友人セリフ | `normal` | `normal` | |
| Scene2 「彼女は笑って…」 | ― | `smile` | |
| Scene2 「声を出して笑った」 | ― | **`laugh`** | 恋人の隣にいる時とは違う笑い方 |
| Scene2 「窓の外に目をやった」 | ― | `away` | 横顔＝視線を外す |
| Scene3 友人（去り際） | `normal` | `normal` | |
| Scene3 「その背中を目で追った」 | **`null`** | ― | 友人が席を外す |
| Scene3 🎤② | 不在 | **`smug`** | 「驚き半分・面白がる半分」＝彼氏を茶化す |
| Scene4 「友人が戻ってくる」 | `normal` | ― | |
| Scene5 「電話に出る」 | **`null`** | ― | 離脱② |
| Scene5 「同じこと言われるんだよね」 | 不在 | `bored` | 拗ねた言い方 |
| Scene5 🎤③ | 不在 | **`laugh`** | 弾んだ声、素の笑顔 |
| Scene6 友人セリフ | `normal` | ― | |
| Scene6 「彼女の表情が、わずかに曇った」 | ― | **`bored`** | セリフの**後**に曇る。予告文にしない |
| Scene7 「売店の列に並ぶ」 | **`null`** | ― | 離脱③ |
| Scene7 🎤④ | 不在 | **`away`** | 「こちらを見ないまま口を開く」 |
| Scene8 🎤⑤（通常） | `normal` | `normal` | |
| Scene8 🎤⑥（警戒） | **`suspicious`** | `bored` | |
| 9-① 🎤⑦ | **`cold`**（未用意なら `suspicious`） | `null` | 彼女は先に歩き出している |
| 9-② 🎤⑧ | `normal` | `null` | 数週間後の場面に彼女はいない |
| 9-③ 🎤⑨ | `null` | **`away`** | 「こちらを見ずに」 |
| 9-④ 🎤⑩ | `null` | **`fluster`** | 緊張と期待が混じる |

これらは `scenes.json` の `stage` / `sprite` に記述済み。台本側には演出指定を書かず、シーンJSONに一元化する。

---


## 8. 技術仕様（Web / PWA）

### 8.1 構成
- 静的サイト（フレームワークは不問）。サーバー処理・DBなし
- **HTTPS配信が必須**：マイク取得 API はセキュアコンテキストでのみ動作する（`localhost` は開発時の例外）
- PWA化（manifest＋Service Worker）。ホーム画面に追加した状態のほうがストレージが保持されやすいため、初回に「ホーム画面に追加」を案内する。キャッシュ範囲は8.6節

### 8.2 録音
- `navigator.mediaDevices.getUserMedia({ audio: CONFIG.AUDIO_CONSTRAINTS })` で取得したストリームを `MediaRecorder` に渡す
- **音声形式を決め打ちしない**：対応形式はブラウザによって異なる（Chrome系は Opus/WebM、Safari は MP4系）。`MediaRecorder.isTypeSupported()` で候補を順に判定し、通った最初の形式を使う。採用された MIME タイプは `mimeType` としてレコードに保存する（再生時に必要）
- 録音停止時は**必ずマイクストリームを止める**（`stream.getTracks().forEach(t => t.stop())`）。掴んだままにすると、端末によっては音声出力の経路が録音向けに切り替わり、その後の再生音が極端に小さくなることがある。またブラウザの録音インジケータも出続ける
- **録音長の上限**（30秒）でオートストップし、無限録音を防ぐ。上限値は5.2の閾値と同様に定数化する。**自動停止したときも録音結果を必ず保存すること**（停止処理の戻り値を捨てると、録音が保存されないまま消える）

**ノイズ対策はブラウザ内蔵の音声処理に任せる**（`js/config.js` の `AUDIO_CONSTRAINTS`）。

| 制約 | 効果 |
|---|---|
| `noiseSuppression: true` | 環境ノイズ（空調・ヒスノイズ）を抑える |
| `echoCancellation: true` | スピーカーからの回り込みを消す。2人で1台を囲むため有効 |
| `autoGainControl: true` | 音量を自動調整。**彼女役と友人役の声量差を吸収できる** |
| `channelCount: 1` | モノラル。容量が半分になる |

端末が対応していれば適用され、非対応でもエラーにはならない（実際に効いたかは `appliedAudioSettings()` で確認できる）。
ML系のノイズ除去（RNNoise等）をWASMで積む選択肢もあるが、**この規模では過剰**。残るノイズは録音環境（静かな場所・端末に近づいて話す）で対処する方針とし、録音画面にその案内を出している。

### 8.3 保存
- **IndexedDB に Blob をそのまま格納**する（キーは `blobKey`）。`localStorage` は容量的に不可
- 再生時は `URL.createObjectURL(blob)`。使い終わったら `revokeObjectURL` で解放する
- 録音開始前に `navigator.storage.persist()` を要求する。許可されない場合、ブラウザのストレージ退去の対象になり**録音データが消えることがある**
- **録音データが失われていた場合の挙動を必ず用意する**：起動時に「10本すべての Blob が実在するか」を検証し、欠けていれば録音画面に戻す（該当分の `status` を `unrecorded` に戻す）。「録音してその日のうちに2人で遊ぶ」想定なので実害は小さいが、無言で壊れる状態は避ける
- 録音データは端末外に一切送信しない。したがって収集する個人データはゼロ

### 8.4 再生（要実機検証）
- 音声再生は**必ずユーザー操作を起点に開始**する。「はじめる」ボタンのタップで `AudioContext` を生成・`resume()` し、以降の自動再生はその文脈で行う（自動再生ポリシー対策）
- **iOS の消音スイッチへの対応は実機確認を必須項目とする**。消音状態だと音が出ない場合があり、本作は「2人でスピーカーから聞く」前提のため致命的。少なくとも**開始画面に「端末の消音を解除してください」を明示**する。それでも鳴らない場合の対策として Web Audio 経由の再生に切り替えられるよう、再生処理は1箇所（例：`playVoice(voiceId)`）に閉じ込めておく
- アプリ内音量調整が必要になった場合は Web Audio の GainNode で行う（HTML の `volume` は iOS で効かない）

### 8.5 権限
- マイク権限はオリジン単位でブラウザが記憶する
- **拒否された場合の画面**を用意する（ブラウザ設定からの再許可手順を案内）。無言で録音ボタンが効かない状態にしない

### 8.6 オフライン対応（完全オフライン）

一度開けば、以降は通信なしで最後まで遊べる状態にする。2人で集まって遊ぶ用途上、屋外や電波の悪い場所でも動くことに実利がある。

**プリキャッシュは2段構え**。BGMが全体の8割を占めるため、これを install に含めると初回の「準備中」が体感に出る。

| 段階 | 内容 | サイズ | タイミング |
|---|---|---|---|
| `CRITICAL` | アプリシェル（HTML/JS/CSS/manifest/アイコン）＋`scenes.json`＋`recordings.json`＋背景4枚＋立ち絵7枚＋効果音4点 | 約1.5MB | **install でブロッキング取得**。これが揃えば起動できる |
| `DEFERRED` | BGM4曲 | 約6.5MB | **activate 後にバックグラウンド取得**。待たない |

取得が済む前にBGMが要求された場合は、`fetch` ハンドラがネットワークから取得してキャッシュに追加する（cache-first＋取得時追記）。したがって**2回目以降は完全オフラインで動く**。

- **録音データはプリキャッシュの対象外**。IndexedDB にあるため Service Worker とは無関係にオフラインで再生できる（8.3節）
- フェッチ戦略は **cache-first**。プリキャッシュ済みのもの以外を取りに行くコードは書かない（外部CDN・Webフォント等を使わない）

**運用上の決めごと**

| 項目 | 決定 |
|---|---|
| 素材の合計サイズ | **10MB以内**を上限とする。超えると初回のプリキャッシュ待ちが体感に出る。画像はWebP、BGMは中ビットレートで書き出す |
| キャッシュの更新 | キャッシュ名にバージョンを含める（例：`kare-v1`）。デプロイのたびに更新し、`activate` で**古いキャッシュを必ず削除する**。これを忘れると素材を差し替えても反映されない |
| 初回の表示 | プリキャッシュ完了までは「準備中」を表示し、完了後に「ホーム画面に追加」を案内する（8.1節。追加した状態のほうがストレージが保持されやすい） |
| 2回目以降 | 通信の有無に関わらず即座に起動する |

### 8.7 配信・公開範囲
- 静的ホスティングで足りる（GitHub Pages / Netlify / Cloudflare Pages 等）
- **限定公開とする**：URLを知っている人だけが遊ぶ前提。`<meta name="robots" content="noindex, nofollow">` を入れ、検索インデックスから外す
- 一般公開しないため、入口の題材注意書きやマイク利用の詳細説明は不要（マイク権限の説明画面は6章のとおり必要）
- ストア審査・年齢レーティングの手続きは発生しない

---

## 9. 開発の進め方（推奨順序）

| # | 工程 | 状態 |
|---|---|---|
| 1 | **データ構造の実装**：4章の構造で `scenes.json` / `recordings.json` を作り、シーン読み込みと分岐ロジック（`js/engine.js`）を用意する | ✅ 完了 |
| 2 | **数値ロジックの検証**：`validate_scenes.py` で5.3の分布・5.4の整合・条件付き行7つ・ボイス10本を確認する（閾値を調整したらスクリプト冒頭の `C` と `js/config.js` の両方を直して再実行） | ✅ 完了（全項目パス） |
| 3 | **周回機能**：変数のみリセットする処理を先に入れる。以降のテストが劇的に速くなる | ✅ 完了（`Engine#reset`） |
| 4 | **名前入力＋置換**：`{{friendName}}` の置換をテキスト表示処理の1箇所に入れる | ✅ 完了（`app.js` の `subst`） |
| 5 | **録音→プレビュー→録り直し→確定**：マイク権限取得、`MediaRecorder`、IndexedDB保存、status遷移 | ✅ 実装済み（実機確認は未） |
| 6 | **本編とのボイス紐付け**：`voiceId` から Blob を引いて該当シーンで再生する | ✅ 実装済み（同上） |
| 7 | **実機確認（iOS/Android）**：8.4の消音スイッチ、8.2の形式差、録音後の再生音量 | ⬜ **次にやること** |
| 8 | **素材（背景・立ち絵・BGM）の組み込み**：現在はCSSのプレースホルダで代替中 | ⬜ 未着手 |
| 9 | **通しテストプレイ**：好感度／警戒度の閾値調整、録音の音量差の確認 | ⬜ 未着手 |
| 10 | スコープ外とした機能（確定後の録り直し・品質チェック等）を必要に応じて追加 | ⬜ 必要に応じて |

この順序にする理由：テキスト部分（1・2）を先に固めることで、素材や録音機能が未完成でもゲームとして最後まで通しプレイでき、早い段階で脚本自体の面白さを検証できるため。実機確認（7）を素材組み込みより前に置くのは、音が鳴らない環境が見つかった場合の影響が最も大きいため。

### 9.1 現時点で検証済みのこと／未検証のこと

| 検証済み | 未検証（実機で確認が必要） |
|---|---|
| 全192ルートを `js/engine.js` で走査し、到達分布が5.3と完全一致 | マイク権限の取得と `MediaRecorder` の実動作 |
| voice_06の再生とバレエンドが全ルートで一致（5.4） | ブラウザごとの音声形式の差（8.2） |
| 1周のボイス再生が4〜6本、表示行数が115〜122行 | 録音後の再生音量、iOSの消音スイッチ（8.4） |
| `recordings.json` の10本が `scenes.json` と完全一致 | IndexedDBへの保存とストレージ永続化の実挙動 |
| 全ファイルがHTTP経由で解決すること | Service Worker のプリキャッシュ |

**未検証の列はすべてブラウザ実機でしか確認できない。** 実装は済んでいるが「動作確認済み」ではない点に注意する。

## 10. 決定事項の一覧

**未決事項はありません。** 設計上の判断はすべて確定済みで、実装に着手できる状態です。
以降に出てくる判断は、実機で動かしてから決めるもの（8.4節のiOS消音スイッチの挙動、5.2節の閾値のバランス調整）に限られます。

### 決定済みの項目（記録）

| 項目 | 決定 | 記載場所 |
|---|---|---|
| エンディングのネタバレ | 開始前に10本すべて録音する（割り切る） | 2.1 |
| 役割分担 | 彼女役6本＋友人役4本。プレイヤーはボイスなし | 1／4.2 |
| 条件付きボイスの未再生 | 許容する（周回の動機とする） | 2.1 |
| 好感度・警戒度のクランプ | 行わない（負値を許容） | 4.3 |
| 実装プラットフォーム | Web / PWA | 8章 |
| 名前の扱い | `{{friendName}}` の入力UIを追加、録音後は変更不可 | 4.4 |
| 録り直し | 確定前の上書きのみ可（`status` に `recorded` を新設） | 4.2 |
| 周回プレイ | 録音・名前を保持して本編のみリセット | 4.3 |
| 本編の尺 | 約9〜11分（テキスト3,429字／1周） | 2.2 |
| シーン数 | 9のまま据え置き（テキスト追加で尺を確保） | 2.1 |
| ②エンドのセリフ | 数週間後の後日談に変更（🎤⑤との重複を解消） | 台本 Scene9 |
| セーブ／中断復帰 | 実装しない（本編は閉じたら最初から） | 2章 |
| ボイス再生中の挙動 | テキスト全文表示＋タップでスキップ可 | 6.1 |
| 公開範囲 | 限定公開（noindex） | 8.7 |
| 正式タイトル | **彼が席を外すたびに** | 1章 |
| オフライン対応 | 完全オフライン（素材まで全プリキャッシュ／上限10MB） | 8.6 |
